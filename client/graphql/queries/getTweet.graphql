query getTweet($url: String!) {
  getTweet(url: $url) {
    likes
    text
    media {
      url
      preview_image_url
      type
    }
    user {
      name
      profile_image_url
      id
      username
      verified
    }
  }
}
