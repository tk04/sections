mutation UpdateMe($input: updateInput!, $token: String!) {
  updateMe(input: $input, token: $token) {
    __typename
    ... on FullUser {
      name
      email
      id
      twitter
      google
      picture
    }
    ... on UserError {
      path
      message
    }
  }
}
