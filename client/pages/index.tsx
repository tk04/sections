import React from "react";

interface indexProps {}

const Index: React.FC<indexProps> = ({}) => {
  return (
    <div className="text-red-500 font-bold text-3xl flex justify-center items-center h-screen w-screen">
      Initial hello
    </div>
  );
};

export default Index;
